def is_prime(n):
    if n == 1:
        return False
    for k in range(2,int(n**0.5)+1):
        if n % k == 0:
            return False
    return True

#nが2未満なら素数ではない、nを2から√nまでの数字で割り切れるなら素数でない。それ以外なら素数
